<!DOCTYPE html>
<html>
<head>
  <title>Uzhgorod Sports Club</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <!-- Оновлений Bootstrap 5 -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>

<body class="bg-dark text-white">
  <nav class="navbar navbar-expand-lg navbar-dark bg-secondary">
    <div class="container d-flex justify-content-between w-100">
      <!-- Left aligned: Navbar brand -->
      <a class="navbar-brand font-weight-bold text-light" href="<%= root_path %>">Спортивний клуб Ужгорода</a>
      
      <!-- Right aligned: Navigation links -->
      <div class="d-flex">
        <ul class="navbar-nav">
          <% if user_signed_in? %>
            <li class="nav-item"><%= button_to 'Вийти', destroy_user_session_path, method: :delete, class: 'nav-link btn-light text-dark' %></li>
          <% else %>
            <li class="nav-item">
              <%= link_to 'Увійти', new_user_session_path, class: 'nav-link btn-light text-dark me-2' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Реєстрація', new_user_registration_path, class: 'nav-link btn-light text-dark' %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container py-5 text-white">
    <div class="text-center">
      <h1 class="display-4">Ласкаво просимо до Спортивного клубу Ужгорода</h1>
      <p class="lead">Об'єднуємо спортсменів та ентузіастів. Тренування, заходи та мотивація – все в одному місці!</p>
    </div>
    <%= yield %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
